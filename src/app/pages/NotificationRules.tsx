import NotificationRuleServer
  from "@/features/notification-rules/components/NotificationRuleView/NotificationRuleServer";

export default function NotificationRules () {
  return (
    <NotificationRuleServer />
  )
}